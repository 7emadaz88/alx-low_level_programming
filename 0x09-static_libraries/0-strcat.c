#include "main.h"

/**
 * _strcat - entry point
 * Description: appends src to dest
 * @src: user input
 * @dest: user destination
 * Return: string
*/

char *_strcat(char *dest, char *src)
{
	return (dest);
}
